{
  "name": "easyread-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EasyRead Chrome extension workspace",
  "scripts": {
    "start": "node server/index.mjs",
    "start:env": "node --env-file=server/.env server/index.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
